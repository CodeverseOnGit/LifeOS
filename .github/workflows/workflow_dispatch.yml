name: Save Data
on:
  workflow_dispatch:   # allows manual trigger
  schedule:
    - cron: '0 0 * * *'  # your daily backup
jobs:
  save-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Show latest data
        run: |
          echo "Reading data.json from repo..."
          cat data.json  # or your real save script